# shiny workflow 

# loading the packages in 
library(shiny)

# the ui 
ui <- fluidPage(
  
)

# loading the server 
server <- function(input, output, session) {
  
}

# loading the app
shinyApp(ui, server)

# this is how one can start the basis of a shiny app 

